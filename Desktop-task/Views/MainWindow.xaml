<Window x:Class="Desktop_task.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Desktop_task"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1100">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- DataGrid with AutoGeneratedColumns -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding ExcelData}" AutoGenerateColumns="True" Width="1000" Height="670" Margin="5">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Class}" Value="По классу">
                            <Setter Property="Background" Value="LightBlue"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Class}" Value="Общее">
                            <Setter Property="Background" Value="LightGreen"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>


        <!-- ToolBar with File Selection Button -->
        <ToolBar Grid.Row="0">
            <Button Content="Open File" Command="{Binding OpenFileCommand}" />
            <Button Content="Parse Excel" Command="{Binding ParseExcelCommand}" />
            <TextBlock Text="{Binding SelectedFilePath}" />
        </ToolBar>

        <!-- List of Loaded Files -->
        <ListBox Grid.Row="2" ItemsSource="{Binding LoadedFiles}" SelectedItem="{Binding SelectedFile}" 
                 Margin="0,10,0,10" VerticalAlignment="Bottom" HorizontalAlignment="Stretch">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding FileName}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Viewbox Grid.Row="1"/>
    </Grid>

    <!-- Style for DataGrid -->
    <Window.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>
</Window>
